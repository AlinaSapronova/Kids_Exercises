<%= form_with(model: challengeday) do |form| %>
  <% if challengeday.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(challengeday.errors.count, "error") %> prohibited this challengeday from being saved:</h2>

      <ul>
        <% challengeday.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :challenge_id, style: "display: block" %>
    <%= form.number_field :challenge_id %>
  </div>

  <div>
    <%= form.label :day_id, style: "display: block" %>
    <%= form.number_field :day_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
