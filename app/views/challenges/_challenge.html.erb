<div class="d-grid gap-2 col-6 mx-auto" id="<%= dom_id challenge %>">
  <button class="btn m-2 p-3 fs-4" >
    <%= link_to "#{challenge.number_of_days} #{challenge.title}", child_challenge_days_path(@child, challenge), class: "link" %></button>

    <%# <%= link_to 'View Days', child_challenge_days_path(@child, challenge) %>

</div>
